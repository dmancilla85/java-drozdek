/// Shared infrastructure: centralized logging (LoggerService), array utilities
/// (ArrayUtils), and the root element interface (DataTypeInterface).
///
/// These utilities are dependency-free and used across every algorithm and
/// data-structure package in the project.
///
/// @since 1.3
package org.drozdek.commons;